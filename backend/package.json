{
  "name": "telegram-user-bot-backend",
  "version": "1.0.0",
  "description": "Backend service for Telegram User Bot",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "jest",
    "build": "rm -rf dist && mkdir dist && cp -r src dist/ && cp package.json dist/ && cp .env dist/ && cd dist && npm install --production"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "sequelize": "^6.35.1",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "telegram": "^2.19.10",
    "winston": "^3.11.0",
    "input": "^1.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0"
  }
} 